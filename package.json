{
  "name": "simple-ecs-restart",
  "version": "1.1.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src --out-dir lib --ignore test.js",
    "coverage": "jest --coverage",
    "lint": "eslint . --fix --format=node_modules/eslint-formatter-pretty",
    "prepublish": "npm run build",
    "test": "jest"
  },
  "author": "Vlad Holubiev",
  "repository": "vladgolubev/simple-ecs-restart",
  "license": "MIT",
  "dependencies": {
    "aws-arn-parser": "^1.0.0",
    "aws-sdk": "^2.34.0",
    "bluebird": "^3.5.0"
  },
  "devDependencies": {
    "aws-sdk-mock": "^1.6.1",
    "babel-cli": "^6.24.0",
    "babel-jest": "^19.0.0",
    "babel-preset-env": "^1.2.2",
    "eslint": "^3.18.0",
    "eslint-config-google": "0.6.0",
    "eslint-formatter-pretty": "^1.1.0",
    "jest": "^19.0.2"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
